import { useAuth } from "@/_core/hooks/useAuth";
import DashboardLayout from "@/components/DashboardLayout";
import WebtoonCover from "@/components/WebtoonCover";
import { Button } from "@/components/ui/button";
import { Checkbox } from "@/components/ui/checkbox";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { AdminEpisode, AdminEpisodeImage, AdminWork, createAdminEpisode, createAdminWebtoon, deleteAdminEpisode, deleteAdminWebtoon, getAdminWebtoon, listAdminWebtoons, updateAdminEpisode, updateAdminWebtoon } from "@/lib/adminRepository";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { BookCopy, ChevronLeft, ChevronRight, FileImage, FolderOpen, ImagePlus, Pencil, Plus, ShieldAlert, Trash2, Upload } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";

type WorkForm = { slug: string; title: string; genre: string; description: string; isPublished: boolean; coverFile?: File; coverPreviewUrl?: string };
type EpisodeForm = { episodeNumber: string; title: string; isPublished: boolean; viewerMode: "scroll" | "swipe" | "both"; readingDirection: "ltr" | "rtl"; imageFiles: File[]; existingImages: AdminEpisodeImage[] };
const blankWork: WorkForm = { slug: "", title: "", genre: "", description: "", isPublished: true };
const blankEpisode: EpisodeForm = { episodeNumber: "", title: "", isPublished: true, viewerMode: "both", readingDirection: "ltr", imageFiles: [], existingImages: [] };
const operatorEmail = "idopublishingcompan@gmail.com";

function validateImages(files: File[]) {
  if (files.length > 1) return "이미지는 한 번에 1컷씩 등록해 주세요.";
  if (files.some(file => file.size > 20 * 1024 * 1024)) return "원본 이미지는 20MB 이하로 선택해 주세요. 등록 시 690px 폭·최대 1280px·2MB 이하로 자동 변환됩니다.";
  return null;
}

export default function Admin() { return <DashboardLayout><AdminContent /></DashboardLayout>; }

function AdminContent() {
  const { user } = useAuth();
  const isAdmin = user?.role === "admin" || user?.email?.toLowerCase() === operatorEmail;
  const queryClient = useQueryClient();
  const { data: works = [], isLoading } = useQuery({ queryKey: ["supabase", "admin", "webtoons"], queryFn: listAdminWebtoons, enabled: isAdmin });
  const [editingWork, setEditingWork] = useState<string | null>(null);
  const [workForm, setWorkForm] = useState<WorkForm>(blankWork);
  const [selectedWorkId, setSelectedWorkId] = useState<string | null>(null);
  const [episodeForm, setEpisodeForm] = useState<EpisodeForm>(blankEpisode);
  const [editingEpisode, setEditingEpisode] = useState<string | null>(null);
  const selectedKey = useMemo(() => ["supabase", "admin", "webtoon", selectedWorkId] as const, [selectedWorkId]);
  const { data: selectedWork } = useQuery({ queryKey: selectedKey, queryFn: () => getAdminWebtoon(selectedWorkId!), enabled: isAdmin && Boolean(selectedWorkId) });
  const refresh = async () => { await queryClient.invalidateQueries({ queryKey: ["supabase", "admin", "webtoons"] }); if (selectedWorkId) await queryClient.invalidateQueries({ queryKey: selectedKey }); };
  const createWork = useMutation({ mutationFn: createAdminWebtoon, onSuccess: async () => { toast.success("작품을 등록했습니다."); setWorkForm(blankWork); await refresh(); }, onError: error => toast.error(error.message) });
  const updateWork = useMutation({ mutationFn: updateAdminWebtoon, onSuccess: async () => { toast.success("작품 정보를 수정했습니다."); setEditingWork(null); setWorkForm(blankWork); await refresh(); }, onError: error => toast.error(error.message) });
  const deleteWork = useMutation({ mutationFn: deleteAdminWebtoon, onSuccess: async () => { toast.success("작품을 삭제했습니다."); setSelectedWorkId(null); await refresh(); }, onError: error => toast.error(error.message) });
  const createEpisode = useMutation({ mutationFn: createAdminEpisode, onSuccess: async () => { toast.success("회차와 이미지가 등록되었습니다."); setEpisodeForm(blankEpisode); await refresh(); }, onError: error => toast.error(error.message) });
  const updateEpisode = useMutation({ mutationFn: updateAdminEpisode, onSuccess: async () => { toast.success("회차 정보를 저장했습니다."); setEditingEpisode(null); setEpisodeForm(blankEpisode); await refresh(); }, onError: error => toast.error(error.message) });
  const deleteEpisode = useMutation({ mutationFn: deleteAdminEpisode, onSuccess: async () => { toast.success("회차를 삭제했습니다."); await refresh(); }, onError: error => toast.error(error.message) });

  if (!isAdmin) return <div className="admin-denied"><ShieldAlert size={34} /><h1>관리자 전용 화면입니다.</h1><p>승인된 운영자 계정으로 로그인한 뒤 다시 시도해 주세요.</p></div>;
  const setWork = <K extends keyof WorkForm>(key: K, value: WorkForm[K]) => setWorkForm(previous => ({ ...previous, [key]: value }));
  const setEpisode = <K extends keyof EpisodeForm>(key: K, value: EpisodeForm[K]) => setEpisodeForm(previous => ({ ...previous, [key]: value }));
  const chooseCover = (file?: File) => { if (!file) return; if (file.size > 7 * 1024 * 1024) return toast.error("표지 이미지는 7MB 이하여야 합니다."); if (!file.type.startsWith("image/")) return toast.error("이미지 파일만 선택할 수 있습니다."); setWork("coverFile", file); setWork("coverPreviewUrl", URL.createObjectURL(file)); };
  const chooseEpisodeImages = (files: FileList | null) => { const values = Array.from(files ?? []); if (!values.length) return; const problem = validateImages(values); if (problem) return toast.error(problem); setEpisodeForm(previous => ({ ...previous, imageFiles: [...previous.imageFiles, ...values] })); };
  const moveExistingImage = (index: number, direction: -1 | 1) => setEpisodeForm(previous => { const destination = index + direction; if (destination < 0 || destination >= previous.existingImages.length) return previous; const existingImages = [...previous.existingImages]; [existingImages[index], existingImages[destination]] = [existingImages[destination], existingImages[index]]; return { ...previous, existingImages }; });
  const submitWork = (event: React.FormEvent) => { event.preventDefault(); const payload = { slug: workForm.slug.toLowerCase().trim(), title: workForm.title, genre: workForm.genre, description: workForm.description, isPublished: workForm.isPublished }; if (!/^[a-z0-9-]+$/.test(payload.slug)) return toast.error("작품 주소는 영문 소문자·숫자·하이픈만 사용할 수 있습니다."); if (editingWork) { const current = works.find(work => work.id === editingWork); updateWork.mutate({ id: editingWork, ...payload, coverFile: workForm.coverFile, currentThumbnailPath: current?.thumbnailPath }); } else { if (!workForm.coverFile) return toast.error("작품 표지 이미지를 선택해 주세요."); createWork.mutate({ ...payload, coverFile: workForm.coverFile }); } };
  const beginEdit = (work: AdminWork) => { setEditingWork(work.id); setSelectedWorkId(work.id); setWorkForm({ slug: work.slug, title: work.title, genre: work.genre, description: work.description, isPublished: work.isPublished }); window.scrollTo({ top: 0, behavior: "smooth" }); };
  const submitEpisode = (event: React.FormEvent) => { event.preventDefault(); if (!selectedWork) return; if (episodeForm.imageFiles.length + (editingEpisode ? episodeForm.existingImages.length : 0) > 70) return toast.error("회차당 이미지는 최대 70컷까지 등록할 수 있습니다."); const number = Number(episodeForm.episodeNumber); if (!Number.isInteger(number) || number < 1) return toast.error("회차 번호는 1 이상의 정수여야 합니다."); if (editingEpisode) { const episode = selectedWork.episodes.find(item => item.id === editingEpisode); if (!episode) return; updateEpisode.mutate({ episode, work: selectedWork.work, episodeNumber: number, title: episodeForm.title, isPublished: episodeForm.isPublished, viewerMode: episodeForm.viewerMode, readingDirection: episodeForm.readingDirection, imageFiles: episodeForm.imageFiles.length ? episodeForm.imageFiles : undefined, imageOrder: episodeForm.imageFiles.length ? undefined : episodeForm.existingImages }); return; } if (!episodeForm.imageFiles.length) return toast.error("세로 뷰어에 표시할 이미지를 선택해 주세요."); createEpisode.mutate({ work: selectedWork.work, episodeNumber: number, title: episodeForm.title, isPublished: episodeForm.isPublished, viewerMode: episodeForm.viewerMode, readingDirection: episodeForm.readingDirection, imageFiles: episodeForm.imageFiles }); };
  const pending = createWork.isPending || updateWork.isPending || createEpisode.isPending || updateEpisode.isPending || deleteWork.isPending || deleteEpisode.isPending;

  return <div className="admin-page">
    <div className="admin-page__heading"><div><p className="eyebrow eyebrow--dark">CONTENT STUDIO</p><h1>명작무료웹툰 운영</h1><p>작품과 회차를 등록하고, 독자에게 공개할 이야기를 관리합니다.</p></div><div className="admin-stat"><span>등록 작품</span><strong>{works.length}</strong></div></div>
    <section className="admin-workbench"><div className="admin-section__heading"><div><span className="admin-section__step">01</span><h2>{editingWork ? "작품 정보 수정" : "새 작품 등록"}</h2></div>{editingWork && <Button variant="ghost" onClick={() => { setEditingWork(null); setWorkForm(blankWork); }}>새 작품 등록으로 전환</Button>}</div><form onSubmit={submitWork} className="work-form"><label className="cover-upload">{workForm.coverPreviewUrl ? <img src={workForm.coverPreviewUrl} alt="선택한 표지 미리보기" /> : editingWork && works.find(work => work.id === editingWork)?.thumbnailUrl ? <img src={works.find(work => work.id === editingWork)?.thumbnailUrl || ""} alt="현재 표지" /> : <><ImagePlus size={25} /><span>표지 이미지<br />선택</span></>}<input type="file" accept="image/jpeg,image/png,image/webp" onChange={event => chooseCover(event.target.files?.[0])} /></label><div className="form-fields"><div className="field-grid"><div><Label htmlFor="work-title">작품 제목</Label><Input id="work-title" value={workForm.title} onChange={event => setWork("title", event.target.value)} required /></div><div><Label htmlFor="work-genre">장르</Label><Input id="work-genre" placeholder="예: 판타지, 로맨스" value={workForm.genre} onChange={event => setWork("genre", event.target.value)} required /></div></div><div><Label htmlFor="work-slug">작품 주소 (영문 소문자·숫자·하이픈)</Label><Input id="work-slug" placeholder="my-masterpiece" value={workForm.slug} onChange={event => setWork("slug", event.target.value)} required /></div><div><Label htmlFor="work-description">작품 소개</Label><Textarea id="work-description" rows={4} value={workForm.description} onChange={event => setWork("description", event.target.value)} required /></div><label className="published-check"><Checkbox checked={workForm.isPublished} onCheckedChange={checked => setWork("isPublished", checked === true)} />독자에게 즉시 공개</label><Button type="submit" disabled={pending}>{editingWork ? <Pencil size={16} /> : <Plus size={16} />}{editingWork ? "작품 정보 저장" : "작품 등록"}</Button></div></form></section>
    <section className="admin-catalog"><div className="admin-section__heading"><div><span className="admin-section__step">02</span><h2>등록된 작품</h2></div><span className="admin-section__note">작품을 선택하면 회차를 관리할 수 있습니다.</span></div>{isLoading ? <p className="admin-placeholder">작품 목록을 불러오는 중입니다.</p> : works.length ? <div className="admin-work-list">{works.map(work => <article key={work.id} className={selectedWorkId === work.id ? "admin-work-card admin-work-card--selected" : "admin-work-card"}><button className="admin-work-card__main" onClick={() => setSelectedWorkId(work.id)}><WebtoonCover src={work.thumbnailUrl} title={work.title} genre={work.genre} /><div><span>{work.genre} · {work.isPublished ? "공개" : "비공개"}</span><h3>{work.title}</h3><p>{work.episodeCount}개 회차</p></div></button><div className="admin-work-card__actions"><Button size="sm" variant="outline" onClick={() => beginEdit(work)}><Pencil size={14} />수정</Button><Button size="icon" variant="ghost" aria-label={`${work.title} 삭제`} onClick={() => { const detail = selectedWork?.work.id === work.id ? selectedWork : undefined; if (detail && window.confirm(`‘${work.title}’과 모든 회차를 삭제할까요?`)) deleteWork.mutate(detail); else if (!detail) toast.message("삭제할 작품을 먼저 선택해 주세요."); }}><Trash2 size={16} /></Button></div></article>)}</div> : <div className="admin-placeholder"><BookCopy size={24} /><p>아직 등록된 작품이 없습니다. 위 양식으로 첫 작품을 추가하세요.</p></div>}</section>
    {selectedWork && <section className="admin-episodes"><div className="admin-section__heading"><div><span className="admin-section__step">03</span><h2>회차 관리</h2><p>{selectedWork.work.title}</p></div><Button variant="ghost" onClick={() => { setSelectedWorkId(null); setEditingEpisode(null); setEpisodeForm(blankEpisode); }}><ChevronLeft size={16} />선택 해제</Button></div><div className="episode-admin-grid"><form onSubmit={submitEpisode} className="episode-form"><div className="episode-form__title"><h3>{editingEpisode ? <Pencil size={18} /> : <Plus size={18} />}{editingEpisode ? "회차 수정" : "새 회차 등록"}</h3>{editingEpisode && <Button type="button" variant="ghost" size="sm" onClick={() => { setEditingEpisode(null); setEpisodeForm(blankEpisode); }}>새 회차 등록</Button>}</div><div className="field-grid"><div><Label htmlFor="episode-number">회차 번호</Label><Input id="episode-number" type="number" min="1" value={episodeForm.episodeNumber} onChange={event => setEpisode("episodeNumber", event.target.value)} required /></div><div><Label htmlFor="episode-title">회차 제목</Label><Input id="episode-title" value={episodeForm.title} onChange={event => setEpisode("title", event.target.value)} required /></div></div><label className="episode-image-upload"><Upload size={22} /><strong>{editingEpisode ? "뷰어 이미지 교체" : "세로 뷰어 이미지 선택"}</strong><span>{editingEpisode ? "새 이미지를 선택하면 현재 이미지 전체를 순서대로 교체합니다." : "한 번에 1컷 · 총 70컷 이하 · 690px 폭 / 최대 1280px / 2MB 이하로 자동 변환"}</span><input type="file" accept="image/jpeg,image/png,image/webp" onChange={event => chooseEpisodeImages(event.target.files)} /></label>{episodeForm.imageFiles.length ? <div className="upload-summary"><FileImage size={16} />{episodeForm.imageFiles.length}컷 대기 중 · 순서대로 누적</div> : null}{editingEpisode && !episodeForm.imageFiles.length && episodeForm.existingImages.length ? <div className="image-order-manager"><p>이미지 순서</p><span>화살표 버튼으로 뷰어 노출 순서를 조정하세요.</span><div>{episodeForm.existingImages.map((image, index) => <figure key={`${image.storagePath}-${index}`}><img src={image.imageUrl} alt={`${episodeForm.title} 이미지 ${index + 1}`} /><figcaption><strong>{index + 1}</strong><span><Button type="button" size="icon" variant="outline" aria-label={`${index + 1}번째 이미지를 앞으로 이동`} disabled={index === 0} onClick={() => moveExistingImage(index, -1)}><ChevronLeft size={14} /></Button><Button type="button" size="icon" variant="outline" aria-label={`${index + 1}번째 이미지를 뒤로 이동`} disabled={index === episodeForm.existingImages.length - 1} onClick={() => moveExistingImage(index, 1)}><ChevronRight size={14} /></Button></span></figcaption></figure>)}</div></div> : null}<div className="viewer-settings"><Label>뷰어 방식</Label><div className="viewer-settings__options"><label><input type="radio" checked={episodeForm.viewerMode === "both"} onChange={() => setEpisode("viewerMode", "both")} />스크롤 기본 + 스와이프</label><label><input type="radio" checked={episodeForm.viewerMode === "scroll"} onChange={() => setEpisode("viewerMode", "scroll")} />스크롤만</label><label><input type="radio" checked={episodeForm.viewerMode === "swipe"} onChange={() => setEpisode("viewerMode", "swipe")} />스와이프 기본</label></div><Label>스와이프 방향</Label><div className="viewer-settings__options"><label><input type="radio" checked={episodeForm.readingDirection === "ltr"} onChange={() => setEpisode("readingDirection", "ltr")} />좌 → 우</label><label><input type="radio" checked={episodeForm.readingDirection === "rtl"} onChange={() => setEpisode("readingDirection", "rtl")} />우 → 좌</label></div></div><label className="published-check"><Checkbox checked={episodeForm.isPublished} onCheckedChange={checked => setEpisode("isPublished", checked === true)} />독자에게 즉시 공개</label><Button type="submit" disabled={pending}>{editingEpisode ? "회차 정보 저장" : "회차와 이미지 등록"}</Button></form><div className="episode-admin-list"><h3><FolderOpen size={18} />등록된 회차 <span>{selectedWork.episodes.length}</span></h3>{selectedWork.episodes.length ? [...selectedWork.episodes].reverse().map(episode => <article key={episode.id}><div><span>{episode.episodeNumber}화 · {episode.isPublished ? "공개" : "비공개"}</span><strong>{episode.title}</strong><small>이미지 {episode.images.length}장</small></div><div className="episode-admin-list__actions"><Button size="icon" variant="ghost" aria-label={`${episode.title} 수정`} onClick={() => { setEditingEpisode(episode.id); setEpisodeForm({ episodeNumber: String(episode.episodeNumber), title: episode.title, isPublished: episode.isPublished, viewerMode: episode.viewerMode, readingDirection: episode.readingDirection, imageFiles: [], existingImages: episode.images }); }}><Pencil size={16} /></Button><Button size="icon" variant="ghost" aria-label={`${episode.title} 삭제`} onClick={() => { if (window.confirm(`‘${episode.title}’ 회차를 삭제할까요?`)) deleteEpisode.mutate(episode); }}><Trash2 size={16} /></Button></div></article>) : <p className="episode-admin-list__empty">등록된 회차가 없습니다.</p>}</div></div></section>}
  </div>;
}

