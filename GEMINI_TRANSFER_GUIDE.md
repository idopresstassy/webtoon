# 명작무료웹툰: 제미나이 이전 안내

이 압축본에는 **명작무료웹툰의 애플리케이션 소스 코드, 데이터베이스 스키마·마이그레이션, 테스트, 화면 구현**이 포함되어 있습니다. 의존성 폴더와 비밀 값은 의도적으로 포함하지 않았습니다.

## 압축본 구성

| 항목 | 포함 여부 | 설명 |
|---|---:|---|
| `client/` | 포함 | React 기반 공개 웹툰·회원·관리자 화면 |
| `server/` | 포함 | tRPC API, 권한 제어, 이미지 업로드·통계 로직 |
| `drizzle/` | 포함 | MySQL/TiDB 스키마와 마이그레이션 SQL |
| `shared/` | 포함 | 공통 타입·상수 |
| `package.json`, `pnpm-lock.yaml` | 포함 | Node.js 의존성·실행 스크립트 |
| `GEMINI_TRANSFER_GUIDE.md` | 포함 | 이 이전 안내서 |
| `assets/ink-painting-hero.jpg` | 포함 | 메인 화면 잉크화 배경 원본 |
| `node_modules/`, `.git/`, 로그 | 제외 | 새 환경에서 다시 설치하거나 불필요한 파일 |
| 비밀 키·환경 변수 | 제외 | 새 배포 환경에서 별도 발급·설정 필요 |
| 운영 데이터·S3 이미지 원본 | 제외 | 현재 서비스의 DB·스토리지에서 별도 내보내기 필요 |

## 로컬 실행

Node.js 20 이상과 pnpm을 준비한 뒤 압축을 해제한 폴더에서 다음을 실행합니다.

```bash
pnpm install
pnpm check
pnpm test
pnpm dev
```

> 이 프로젝트는 기존에 Manus 제공 인증·스토리지·DB 환경에서 동작했습니다. 제미나이에서 코드를 검토·수정하는 것은 가능하지만, 실제 배포를 위해서는 아래 외부 연동을 새 환경에 맞게 바꾸어야 합니다.

## 새 환경에서 준비할 항목

| 구분 | 현재 구현 | 제미나이 이전 시 조치 |
|---|---|---|
| 인증 | 기존 제공 OAuth 흐름과 역할 기반 접근 | Firebase Authentication, Google OAuth 또는 자체 인증으로 교체하고 운영자 역할을 설정합니다. |
| 데이터베이스 | MySQL/TiDB + Drizzle | 새 MySQL 인스턴스를 만들고 `drizzle/migrations/`의 SQL을 적용합니다. |
| 이미지 저장 | 기존 S3 호환 스토리지 | Google Cloud Storage, Firebase Storage 또는 별도 S3 버킷으로 업로드 흐름을 변경합니다. |
| 환경 변수 | 앱·인증·DB·스토리지 관련 비밀 값 | `.env`를 새로 만들고 실제 값을 배포 플랫폼의 비밀 관리 기능에 등록합니다. |
| 분석 | `readingEvents` 테이블의 익명 열람 기록 | DB 테이블을 함께 이전하면 관리자 통계를 이어서 사용할 수 있습니다. |

## 반드시 별도로 내보낼 데이터

소스 압축본에는 이용자·작품 데이터가 들어 있지 않습니다. 기존 운영 데이터베이스에서 최소한 아래 테이블을 내보낸 뒤 새 DB에 가져와야 합니다.

| 테이블 | 용도 |
|---|---|
| `users` | 회원·운영자 계정 및 역할 |
| `webtoons` | 작품 제목, 소개, 장르, 표지 URL |
| `episodes` | 회차 제목·번호·공개 상태 |
| `episodeImages` | 세로 뷰어 이미지의 순서와 URL |
| `readingEvents` | 관리자 열람 통계 |

이미지 파일도 현재 스토리지에서 별도로 내려받아 새 스토리지에 업로드해야 합니다. 이전 후에는 표지와 회차 이미지 URL을 새 저장소 URL로 갱신해야 합니다.

## 제미나이에 전달할 권장 요청문

```text
첨부한 명작무료웹툰 프로젝트는 React, Express, tRPC, Drizzle(MySQL) 기반입니다.
기존 제공 OAuth와 S3 호환 스토리지 의존성을 Firebase Authentication과 Firebase Storage로 교체하고,
비회원은 웹툰을 무료로 감상하며 운영자만 /admin에 접근하도록 유지해 주세요.
GEMINI_TRANSFER_GUIDE.md의 데이터 이전·환경 변수·이미지 URL 마이그레이션 조건을 따르세요.
```

## 이전 전 확인 목록

- 새 인증 서비스에서 `idopublishingcompan@gmail.com` 계정을 운영자로 지정합니다.
- 새 데이터베이스에 마이그레이션과 운영 데이터를 적용합니다.
- 표지·회차 이미지를 새 스토리지에 업로드하고 URL을 갱신합니다.
- `pnpm check`와 `pnpm test`를 실행합니다.
- 비회원 감상, 일반 회원의 관리자 차단, 운영자 로그인과 작품·회차 업로드를 확인합니다.
